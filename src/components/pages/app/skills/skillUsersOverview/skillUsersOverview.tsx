import React from 'react';

type SkillUsersOverviewProps = {
    match: { params: { skillId: string, email: string } }
};
export const SkillUsersOverview: React.FunctionComponent<SkillUsersOverviewProps> = ({}) => {
    return (
        <div>

        </div>
    );
};